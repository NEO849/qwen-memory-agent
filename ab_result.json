{
  "task": "get_orders tenant isolation",
  "model": "qwen-plus",
  "temperature": 0,
  "k": 5,
  "distilled_lesson": "Keep only resources where resource['tenant_id'] == user['tenant_id']; never compare against user['id'] when tenant isolation is required.",
  "injected_form": "canonical (deterministic replay of the remembered fix)",
  "used_fallback": true,
  "injected_lesson": "Orders are tenant-scoped. The user dict carries user['tenant_id'] (a string like 'acme'), separate from the numeric user['id']. In get_orders, return only the orders whose order['tenant_id'] == user['tenant_id'] \u2014 compare against user['tenant_id'], never user['id'].",
  "note": "Same model, same temperature=0, same hidden test the agent never sees. The only variable is memory. Both arms are live Qwen runs; arm B's injected rule is the remembered human fix (distilled_lesson shows Qwen's live paraphrase of it). temp-0 code-gen still varies run-to-run, so arm B injects the fix in its concrete form for a stable demo \u2014 re-run `python -m harness.ab_runner --k 5` to reproduce.",
  "lesson": "Keep only resources where resource['tenant_id'] == user['tenant_id']; never compare against user['id'] when tenant isolation is required.",
  "arm_a_no_memory": {
    "arm": "A",
    "k": 5,
    "green": 0,
    "red": 5,
    "pass_rate": 0.0,
    "sample_code": "def get_orders(db, user):\n    return [order for order in db.all_orders() if order.get('user_id') == user['id']]"
  },
  "arm_b_with_memory": {
    "arm": "B",
    "k": 5,
    "green": 5,
    "red": 0,
    "pass_rate": 1.0,
    "sample_code": "def get_orders(db, user):\n    return [order for order in db.all_orders() if order['tenant_id'] == user['tenant_id']]"
  },
  "delta_pass_rate": 1.0
}